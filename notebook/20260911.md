## 20260911

### 1. Skytrax Review Code (continued)

#### Task 1: Understand skytrax review architecture, source? ingestion? landing?

(https://github.com/MarkPhamm/skytrax_reviews_extract_load)

- where does Airflow play a part in this? (cont)
    - Airflow on Astronomer Runtime (Docker):
        - Set up the flow with a DAGs design (see the DAGs setup from folder /assets/airflow)
        - crawl → process → snowflake: 
            - data scrawling: retrieves data by digging deep into the internet or a specific target. carried out by bots or software called crawler

- where does Terraform play a part in this? (will look into it deeper both standing alone and in this project specifically)
    - automate and manage cloud infrastructure: allows you to define your infrastructure in code and then create and maintain those resources on various cloud platforms like AWS, Azure, and Google Cloud
    - in this project:
        - provider: aws
            - providers allow Terraform to interact with cloud providers, SaaS providers, and other APIs
        - resource: s3
            - each resource block describes one or more infrastructure objects, such as a storage account
        - vars: 
            - if set in root module: can set var values using CLI options and environment variables
            - if set in child modules (which this project does in /terraform/aws/variables.tf): the calling module should pass values in the module block

- INGESTION: use Custom Python scraper
    -> reason: AirlineQuality.com had no public API (and is now permanently closed) — Fivetran/Airbyte couldn’t land HTML reviews; polite crawl + parse into a replayable S3 archive instead.

- LANDING: AWS S3
    -> reason: Replayable raw archive; decouple scrape from load; cheaper than warehouse history.

